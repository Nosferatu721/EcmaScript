// Se implemento el Metodo include en los Arrays
let numbers = [1, 2, 3, 8, 5];
let n = 8;
if (numbers.includes(n)) {
  // console.log("Si se encuentra el " + n);
} else {
  // console.log("No se encuentra el " + n);
}

// Calcular un exponente con **
let base = 2;
let expo = 4;
let result = base ** expo;

// console.log(result);
