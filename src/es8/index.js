const data = {
  frontend: "Oscar",
  backend: "Isabel",
  design: "Ana",
};

const entries = Object.entries(data);
console.log(entries);
console.log(entries.length);

const data1 = {
  frontend: "Oscar",
  backend: "Isabel",
  design: "Ana",
};

// Tomar los valores de cada item del Objeto y insertarlos en un Array
const values = Object.values(data1);
console.log(values);
